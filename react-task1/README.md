## 📌 What I Built

A **Profile Card component** that:
- Displays name, role, and hobbies
- Uses props to receive data
- Uses state to update text on button click

---

## 🧠 Concepts Learned

### 1️⃣ What is React?
- React is a JavaScript library used to build **user interfaces**
- UI is divided into **components**
- UI updates automatically when data (state) changes

---

### 2️⃣ React App Structure (Vite)

src/

├── main.jsx → Entry point

├── App.jsx → Parent component

├── ProfileCard.jsx→ Child component

└── index.css → Styles

---

### 3️⃣ Component
- A component is a **JavaScript function**
- It returns JSX (UI)

Example:
```jsx
function App() {
  return <h1>Hello React</h1>
}